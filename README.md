# 2D Spline Animation
CatMullRom curve with animated dot. To use it on chrome console:

let myPoints = spline.generateSplinePath();
drawOnMe.drawSpline(myPoints, "green", 2); let path = spline.generateSplinePath();
animateDot(spline.points, path, drawOnMe);
